% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.lreg.R
\name{plot.lreg}
\alias{plot.lreg}
\title{Plotting logistic regression}
\usage{
\method{plot}{lreg}(x, points = FALSE, dot.size = 1, dot.alpha = 0.5, ci = TRUE, ...)
}
\arguments{
\item{x}{an object of type \code{c("lreg", "lm")}}

\item{points}{points is TRUE if data points are to be shown, default is FALSE}

\item{dot.size}{size of data points, default is 1}

\item{dot.alpha}{transparency of data points, default is 0.5}

\item{ci}{if confidence intervals are to be shown, default is TRUE}

\item{...}{additional arguments to be added to adjust graphic}
}
\value{
a plot of class \code{ggplot2}
}
\description{
The plot function provides single plot that shows separate graphs of the relationship
between each predictor and the outcome, controlling for the other predictors from the
lreg function
}
\details{
The \code{plot.lreg} function is uses ggeffects package to plot the results of the lreg function from the
qacReg package. The ggeffects package aims to easily calculate marginal effects and adjusted predictions
at representative values of covariates from statistical models, i.e. predictions generated by a model when
one holds the controlled dependent variable(s) constant and varies the independent variable(s).
To see more details and gain better understanding of the ggeffects package see
\link[https://strengejacke.github.io/ggeffects/]{here}
}
\examples{
mtcars$am <- factor(mtcars$am)
fit <- lreg(am ~ hp + wt + mpg, mtcars)
plot(fit)
}
\seealso{
\link[ggeffects]{ggeffect}
\link[ggplot2]{ggplot}
}
